<div class="max-w-2xl mx-auto py-8 px-4">

  <h1 class="text-3xl font-bold mb-2">
    <%= @lesson.title %>
  </h1>

  <h2 class="text-xl font-semibold mb-6 text-gray-600">
    <%= @lesson_section.title %>
  </h2>

  <%= render partial: @exercise.partial_path,
        locals: { exercise: @exercise, lesson: @lesson, lesson_section: @lesson_section, revealed: params[:revealed] } %>

</div>